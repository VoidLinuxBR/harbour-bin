# Template PKGFILE - instalação local Harbour
# maintainer: Vilmar Catafesta <vcatafesta@gmail.com>"

pkgname=harbour-bin
pkgver=3.4
pkgrel=5
#build_style=manual-install
short_desc="Harbour Compiler (xBase language)"
license="GPL-2.0-only"
homepage="https://harbour.github.io/"
depends="ncurses-libtinfo-devel slang libX11 gpm pcre2 zlib"
build_style=manual-install
build_style=local

package() {
   local dirs=("usr" "etc" "opt")
   #   echo "DESTDIR=$DESTDIR"
   #   echo "pkgdir=$pkgdir"
   #   echo "srcdir=$srcdir"
   #   echo "_pkgsrc=$_pkgsrc"

   cd ${srcdir}/${pkgname}-${pkgver}
   for dir in "${dirs[@]}"; do
      if [ -d "${_pkgsrc}/${dir}" ]; then
         cp -a "${_pkgsrc}/${dir}" "$DESTDIR/"
      fi
   done
}
